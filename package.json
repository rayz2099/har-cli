{
  "name": "har-cli",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-only",
  "type": "module",
  "packageManager": "bun@1.3.11",
  "bin": {
    "har-cli": "./dist/har-cli"
  },
  "scripts": {
    "build": "bun build --compile ./src/cli.ts --outfile dist/har-cli",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
